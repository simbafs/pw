# pw

offline password generator based on HMAC

pw use hmac(masterkey, site name) to generate a human memerable password. It read the site specific config (such is require upper case, digits, or special characters) and generate a password satisfy the requirement.
